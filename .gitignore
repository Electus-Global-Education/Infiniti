# .gitignore

# Environment Variables
# Ensure your .env files containing secrets are not committed!
.env
.env.*
!.env.example # Often good to commit an example .env file
.env_gemini
.env_vector_store

#keys
keys/*
!keys/.gitkeep

# Django
*.log
*.pot
*.pyc
__pycache__/
local_settings.py
db.sqlite3
db.sqlite3-journal
media/ # Usually, user-uploaded media is not versioned, but depends on strategy
staticfiles/ # Collected static files, should be generated during deployment

# Python
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# Virtualenv
.venv/
venv/
ENV/
env/
env.bak/
venv.bak/

# Docker
# Docker-specific files that might be generated locally
docker-compose.override.yml # Often used for local overrides not meant for repo
.dockerignore # This file itself should be committed, but listing for completeness if generated by tools

# IDEs and Editors
.idea/ # JetBrains IDEs (PyCharm, IntelliJ)
.vscode/ # Visual Studio Code
*.swp # Vim swap files
*~ # Backup files
*.sublime-project
*.sublime-workspace

# Operating System Files
.DS_Store # macOS
Thumbs.db # Windows
ehthumbs.db # Windows

# Coverage reports
.coverage
.coverage.*
htmlcov/
coverage.xml

# Jupyter Notebook
.ipynb_checkpoints

# Specific to your project if needed:
# For example, if you have local data files you don't want to commit
# local_data/
# output_files/

# If you are using SQLite for any local testing outside Docker (though your setup uses Postgres)
# *.sqlite3
